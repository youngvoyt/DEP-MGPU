# Лабораторная работа 2.1: Проведение полного цикла EDA и создание аналитического отчета с гипотезами

## Цель работы

Провести полный цикл разведочного анализа данных (EDA) на реальном наборе данных, научиться формулировать и проверять гипотезы с помощью библиотеки Pandas, а также создавать визуализации для подкрепления своих выводов. В результате выполнения работы будет создан полноценный аналитический отчет.

## 👤 Автор

**Работу выполнил студент МГПУ**

**Войт Иван Иванович**

**Группа:** БД-251м

## 📓 Рабочая тетрадь

[bi_lw_02_EDA_student.ipynb](bi_lw_02_EDA_student.ipynb)

## 📊 Описание данных

Набор данных `cardio_train.csv` содержит информацию о пациентах, их медицинских показателях и наличии сердечно-сосудистых заболеваний (ССЗ).

**Признаки данных:**
- `id` - Уникальный идентификатор
- `age` - Возраст в днях
- `gender` - Пол (1 - женщины, 2 - мужчины)
- `height` - Рост в см
- `weight` - Вес в кг
- `ap_hi` - Систолическое (верхнее) артериальное давление
- `ap_lo` - Диастолическое (нижнее) артериальное давление
- `cholesterol` - Уровень холестерина (1: норма, 2: выше нормы, 3: значительно выше нормы)
- `gluc` - Уровень глюкозы (1: норма, 2: выше нормы, 3: значительно выше нормы)
- `smoke` - Курение (0: нет, 1: да)
- `alco` - Употребление алкоголя (0: нет, 1: да)
- `active` - Физическая активность (0: нет, 1: да)
- `cardio` - **Целевая переменная:** наличие ССЗ (0: нет, 1: да)

## 🔍 Пояснения к решению (Вариант 10)

### Пункт 1: Сравнение среднего роста у курящих и некурящих

**Метод решения:**
Использована группировка данных по признаку `smoke` с последующим вычислением среднего значения роста (`height`) для каждой группы. Результат выводится с указанием, у какой группы средний рост выше и на сколько сантиметров.

**Код:**
```python
mean_height_smoke = df_cleaned.groupby('smoke')['height'].mean()
```

### Пункт 2: 5 самых пожилых пациентов с диастолическим давлением выше 90

**Метод решения:**
1. Фильтрация данных по условию `ap_lo > 90`
2. Сортировка отфильтрованных данных по возрасту (`age_years`) в порядке убывания
3. Выбор первых 5 записей с помощью метода `nlargest()`
4. Вывод релевантных столбцов: возраст, давление (верхнее и нижнее), пол, наличие ССЗ

**Код:**
```python
oldest_high_ap_lo = df_cleaned[df_cleaned['ap_lo'] > 90].nlargest(5, 'age_years')
```

### Пункт 3: Доля физически неактивных пациентов среди людей без ССЗ

**Метод решения:**
1. Выделение подмножества пациентов без ССЗ (`cardio == 0`)
2. Подсчет количества физически неактивных (`active == 0`) среди них
3. Вычисление доли как отношение количества неактивных к общему количеству пациентов без ССЗ
4. Результат представлен в процентах и в виде дроби для наглядности

**Код:**
```python
no_cardio = df_cleaned[df_cleaned['cardio'] == 0]
inactive_no_cardio = no_cardio[no_cardio['active'] == 0]
proportion = len(inactive_no_cardio) / len(no_cardio) * 100
```

### Пункт 4: Создание признака pressure_category

**Метод решения:**
1. Создана функция `categorize_pressure()`, которая классифицирует систолическое давление:
   - `'Optimal'` - если `ap_hi < 120`
   - `'Normal'` - если `120 <= ap_hi < 140`
   - `'High'` - если `ap_hi >= 140`
2. Применение функции ко всем значениям `ap_hi` с помощью метода `apply()`
3. Подсчет количества пациентов в каждой категории с помощью `value_counts()`
4. Дополнительно выведено процентное распределение по категориям

**Код:**
```python
def categorize_pressure(ap_hi):
    if ap_hi < 120:
        return 'Optimal'
    elif ap_hi < 140:
        return 'Normal'
    else:
        return 'High'

df_cleaned['pressure_category'] = df_cleaned['ap_hi'].apply(categorize_pressure)
```

**Обоснование категорий:**
- Категории основаны на клинических рекомендациях по классификации артериального давления
- Оптимальное давление: < 120 мм рт.ст.
- Нормальное давление: 120-139 мм рт.ст.
- Высокое давление: ≥ 140 мм рт.ст.

### Пункт 5: Сравнение медианного ИМТ у женщин с ССЗ и без ССЗ

**Метод решения:**
1. Фильтрация данных по полу: выделение женщин (`gender == 1`)
2. Разделение женщин на две группы: с ССЗ (`cardio == 1`) и без ССЗ (`cardio == 0`)
3. Вычисление медианного значения ИМТ (`bmi`) для каждой группы
4. Сравнение значений с выводом разницы и указанием, у какой группы медианный ИМТ выше

**Код:**
```python
women = df_cleaned[df_cleaned['gender'] == 1]
median_bmi_women_no_cardio = women[women['cardio'] == 0]['bmi'].median()
median_bmi_women_cardio = women[women['cardio'] == 1]['bmi'].median()
```

**Почему медиана, а не среднее:**
Медиана более устойчива к выбросам, что важно при анализе медицинских данных, где могут присутствовать аномальные значения ИМТ.

## 🛠️ Используемые библиотеки

- `pandas` - для работы с данными
- `numpy` - для численных вычислений
- `matplotlib` - для базовой визуализации
- `seaborn` - для расширенной визуализации

## 📝 Структура работы

1. **Загрузка и первичный осмотр данных** - изучение структуры датасета
2. **Предобработка и очистка данных** - удаление выбросов, преобразование признаков
3. **Одномерный анализ** - анализ распределения каждого признака
4. **Двумерный и многомерный анализ** - поиск взаимосвязей и проверка гипотез
5. **Индивидуальное задание (Вариант 10)** - выполнение 5 специфических задач анализа

## 📈 Основные результаты EDA

- Классы целевой переменной `cardio` сбалансированы (примерно 50/50)
- Выявлена положительная корреляция между возрастом, давлением, холестерином и наличием ССЗ
- Уровень холестерина значительно влияет на риск ССЗ (с 40% до 76% при росте уровня)
- Мужчины более склонны к курению и употреблению алкоголя, чем женщины

