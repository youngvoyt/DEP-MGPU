# 📊 ПОЛНОЕ ОПИСАНИЕ РАБОТЫ ПО МАШИННОМУ ОБУЧЕНИЮ

**Автор:** Студент МГПУ Войт Иван Иванович  
**Дисциплина:** Машинное обучение  
**Дата:** 2024

---

## 📋 СОДЕРЖАНИЕ

1. [Общее описание работы](#общее-описание-работы)
2. [Часть 1: Задача классификации](#часть-1-задача-классификации)
3. [Часть 2: Задача регрессии](#часть-2-задача-регрессии)
4. [Часть 3: Машинное обучение](#часть-3-машинное-обучение)
5. [Выводы и рекомендации](#выводы-и-рекомендации)

---

## 🎯 ОБЩЕЕ ОПИСАНИЕ РАБОТЫ

### Цель работы
Выполнение комплексного исследования данных с применением методов машинного обучения для решения двух задач:
- **Классификация**: предсказание типа автомобиля на основе характеристик объявления
- **Регрессия**: предсказание количества лайков комментариев на YouTube

### Структура работы
Работа состоит из трех основных частей:
1. **Исследовательский анализ данных (EDA)** для задачи классификации
2. **Исследовательский анализ данных (EDA)** для задачи регрессии
3. **Обучение и сравнение моделей машинного обучения**

### Используемые технологии
- **Python** - основной язык программирования
- **Pandas** - работа с данными
- **NumPy** - численные вычисления
- **Matplotlib & Seaborn** - визуализация
- **Scikit-learn** - метрики и разделение данных
- **CatBoost, XGBoost, LightGBM, YDF** - алгоритмы машинного обучения
- **H2O** - платформа машинного обучения с веб-интерфейсом

---

## 📊 ЧАСТЬ 1: ЗАДАЧА КЛАССИФИКАЦИИ

### 1.1. Описание датасета

**Датасет:** `autos.csv`  
**Размер:** 371,528 строк × 21 столбец  
**Целевая переменная:** `vehicleType` (8 классов)

#### Типы признаков:
- **Категориальные**: бренд, модель, тип топлива, коробка передач, тип продавца
- **Непрерывные**: цена, год регистрации, мощность двигателя, пробег
- **Дата/время**: дата скачивания, дата создания, последний просмотр

#### Классы целевой переменной:
1. `limousine` (28.7%) - седаны
2. `kleinwagen` (24.0%) - малолитражные
3. `kombi` (20.2%) - универсалы
4. `bus` (9.1%) - автобусы
5. `cabrio` (6.9%) - кабриолеты
6. `coupe` (5.7%) - купе
7. `suv` (4.4%) - внедорожники
8. `andere` (1.0%) - другие

### 1.2. Установка и импорт библиотек

#### Код:
```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
```

#### Объяснение:
- **Pandas** (`pd`) - для работы с табличными данными (DataFrame)
- **NumPy** (`np`) - для численных операций и работы с массивами
- **Matplotlib** (`plt`) - для создания графиков
- **Seaborn** (`sns`) - для более красивых статистических графиков

#### Настройки визуализации:
```python
sns.set_style("whitegrid")  # Сетка на белом фоне
plt.rcParams['figure.figsize'] = (14, 8)  # Размер графиков по умолчанию
```

**Логика:** Эти настройки обеспечивают единообразный и читаемый стиль всех графиков в работе.

### 1.3. Загрузка данных

#### Код:
```python
df_class = pd.read_csv('autos.csv')
```

#### Объяснение:
- `pd.read_csv()` - функция Pandas для чтения CSV-файлов
- Результат сохраняется в переменную `df_class` (DataFrame)
- DataFrame - это двумерная структура данных, похожая на таблицу Excel

#### Логика работы:
1. Файл читается построчно
2. Первая строка интерпретируется как заголовки столбцов
3. Данные преобразуются в структурированный формат для анализа

### 1.4. Описательные статистики

#### Код:
```python
numeric_cols_class = df_class.select_dtypes(include=[np.number]).columns.tolist()
df_class[numeric_cols_class].describe()
```

#### Объяснение:
- `select_dtypes(include=[np.number])` - выбирает только числовые столбцы
- `.describe()` - вычисляет описательные статистики:
  - **count** - количество непустых значений
  - **mean** - среднее значение
  - **std** - стандартное отклонение
  - **min/max** - минимальное/максимальное значение
  - **25%, 50%, 75%** - квартили (медиана = 50%)

#### Зачем это нужно:
Описательные статистики дают первое представление о данных:
- Показывают масштаб значений
- Выявляют потенциальные проблемы (выбросы, пропуски)
- Помогают понять распределение данных

### 1.5. Анализ целевой переменной

#### Код:
```python
target_dist = df_class[target_col_class].value_counts()
target_dist_pct = df_class[target_col_class].value_counts(normalize=True) * 100
```

#### Объяснение:
- `value_counts()` - подсчитывает частоту каждого класса
- `normalize=True` - возвращает проценты вместо абсолютных значений
- Результат показывает распределение классов

#### Визуализация:
1. **Countplot (столбчатая диаграмма)** - показывает абсолютное количество каждого класса
2. **Pie chart (круговая диаграмма)** - показывает процентное распределение

#### Выводы из анализа:
- **Классы несбалансированы**: соотношение min/max = 0.035
- Самый частый класс (`limousine`) встречается в 28.7% случаев
- Самый редкий класс (`andere`) встречается только в 1% случаев
- **Проблема**: Несбалансированность может привести к тому, что модель будет плохо предсказывать редкие классы

**Рекомендация:** Использовать взвешивание классов или oversampling при обучении моделей.

### 1.6. Анализ пропущенных значений

#### Код:
```python
missing = df_class.isnull().sum()
missing_pct = (missing / len(df_class)) * 100
```

#### Объяснение:
- `isnull()` - возвращает True для пропущенных значений
- `.sum()` - подсчитывает количество пропусков по столбцам
- Вычисляется процент пропусков от общего количества строк

#### Результаты анализа:
- `notRepairedDamage`: 19.4% пропусков (информация о повреждениях часто не указывается)
- `vehicleType`: 10.2% пропусков (**критично** - это целевая переменная!)
- `fuelType`: 9.0% пропусков
- `model`: 5.5% пропусков
- `gearbox`: 5.4% пропусков

#### Логика обработки:
1. **Для целевой переменной**: удаляем строки с пропусками (не можем предсказывать неизвестное)
2. **Для остальных признаков**: заполняем пропуски:
   - Категориальные → 'Unknown'
   - Числовые → медиана (более устойчива к выбросам, чем среднее)

### 1.7. Анализ дубликатов

#### Код:
```python
full_duplicates = df_class.duplicated().sum()
df_class = df_class.drop_duplicates()
```

#### Объяснение:
- `duplicated()` - находит полностью идентичные строки
- `drop_duplicates()` - удаляет дубликаты, оставляя первую копию

#### Результат:
- Дубликатов не найдено (0 строк)
- Это хорошо - данные уникальны

**Зачем это важно:** Дубликаты могут искажать результаты обучения, создавая "ложную" уверенность модели.

### 1.8. Распределения числовых признаков

#### Код:
```python
sns.histplot(data=df_class, x=col, kde=True, bins=50)
```

#### Объяснение:
- `histplot()` - создает гистограмму распределения
- `kde=True` - добавляет кривую плотности (Kernel Density Estimation)
- `bins=50` - количество интервалов для группировки данных

#### Анализ распределений:

1. **price (цена)**:
   - **Правостороннее распределение** (много дешевых, мало дорогих)
   - Большинство автомобилей в диапазоне 0-50,000 EUR
   - Немного очень дорогих автомобилей (выбросы)

2. **yearOfRegistration (год регистрации)**:
   - Распределение смещено к более новым годам
   - Пик около 2000-2010 годов
   - Логично для подержанных автомобилей

3. **powerPS (мощность)**:
   - **Нормальное распределение** с пиком около 100-150 л.с.
   - Большинство автомобилей в среднем диапазоне мощности

4. **kilometer (пробег)**:
   - **Равномерное распределение**
   - Логично для подержанных автомобилей разных возрастов

#### Выводы:
- Распределения показывают, что большинство объявлений - о подержанных автомобилях среднего класса
- Правосторонние распределения указывают на наличие выбросов

### 1.9. Анализ выбросов и аномалий

#### Код:
```python
print(f"Цены = 0: {(df_class['price'] == 0).sum():,}")
print(f"Цены > 1,000,000: {(df_class['price'] > 1000000).sum():,}")
```

#### Объяснение:
Выбросы - это значения, которые значительно отличаются от остальных данных. Они могут быть:
- **Ошибками ввода** (нулевые цены, некорректные годы)
- **Редкими, но реальными случаями** (коллекционные автомобили)

#### Найденные аномалии:

1. **Цены = 0**: 10,778 (2.9%)
   - Вероятно, ошибки ввода или специальные предложения
   - **Действие**: удалить или заменить

2. **Цены > 1,000,000**: 65 (0.02%)
   - Редкие коллекционные автомобили или ошибки
   - **Действие**: ограничить максимальной разумной ценой

3. **Год < 1950 или > 2020**: 403 случая
   - Некорректные годы (1000, 9999) - явные ошибки
   - **Действие**: удалить или ограничить диапазоном 1950-2020

4. **Мощность = 0**: 40,820 (11%)
   - Возможно, электромобили или ошибки
   - **Действие**: проверить и обработать отдельно

5. **Мощность > 1000**: 306 случаев
   - Экстремально мощные автомобили или ошибки
   - **Действие**: ограничить разумным максимумом

#### Логика обработки:
```python
df_export_class = df_export_class[(df_export_class['price'] > 0) & 
                                   (df_export_class['price'] < 500000)]
df_export_class = df_export_class[(df_export_class['yearOfRegistration'] >= 1950) & 
                                   (df_export_class['yearOfRegistration'] <= 2020)]
```

**Зачем это важно:** Выбросы могут сильно исказить результаты обучения моделей, особенно регрессионных.

### 1.10. Матрица корреляции

#### Код:
```python
correlation_matrix = df_class[corr_cols].corr()
sns.heatmap(correlation_matrix, annot=True, fmt='.2f', cmap='coolwarm')
```

#### Объяснение:
- `corr()` - вычисляет корреляцию Пирсона между всеми парами признаков
- Корреляция показывает линейную зависимость между признаками (-1 до +1)
- `heatmap` - визуализирует матрицу корреляций цветом

#### Интерпретация:
- **+1.0** - полная положительная корреляция (признаки растут вместе)
- **0.0** - нет корреляции
- **-1.0** - полная отрицательная корреляция (один растет, другой падает)
- **> 0.5 или < -0.5** - сильная корреляция

#### Результаты:
- Сильных корреляций не найдено (|r| < 0.5)
- Это **хорошо** для моделей:
  - Отсутствие мультиколлинеарности
  - Все признаки могут быть полезны
  - Модели могут находить сложные паттерны

**Мультиколлинеарность** - это когда признаки сильно коррелируют друг с другом, что может ухудшить качество модели.

### 1.11. Группировка данных и сводные таблицы

#### Код:
```python
brand_price = df_class.groupby('brand')['price'].agg(['mean', 'count', 'median'])
```

#### Объяснение:
- `groupby('brand')` - группирует данные по брендам
- `['price']` - выбирает столбец для анализа
- `.agg(['mean', 'count', 'median'])` - применяет несколько агрегатных функций:
  - `mean` - средняя цена
  - `count` - количество автомобилей
  - `median` - медианная цена (более устойчива к выбросам)

#### Результаты:
- **Премиум бренды** (Porsche, Mercedes-Benz, BMW) имеют значительно более высокие цены
- **Экономичные бренды** (Opel, Ford, Volkswagen) находятся в среднем ценовом диапазоне
- **Вывод**: Бренд является важным фактором для предсказания типа автомобиля

#### Визуализация:
```python
sns.barplot(x=brand_price_viz.values, y=brand_price_viz.index)
```
- Столбчатая диаграмма показывает среднюю цену по брендам
- Легко видеть разницу между премиум и экономичными брендами

### 1.12. Визуализация динамики изменений

#### Код:
```python
df_class['dateCreated_dt'] = pd.to_datetime(df_class['dateCreated'], errors='coerce')
daily_counts = df_temp.groupby('dateCreated_date').size()
```

#### Объяснение:
- `pd.to_datetime()` - преобразует строки в объекты даты/времени
- `errors='coerce'` - преобразует невалидные даты в NaN вместо ошибки
- `groupby().size()` - подсчитывает количество записей по датам

#### Созданные графики:

1. **Динамика создания объявлений по дням**:
   - Линейный график показывает активность продавцов во времени
   - Выявляет сезонные колебания и тренды

2. **Динамика средней цены по дням**:
   - Показывает рыночные тренды
   - Может выявить сезонность (например, цены выше весной)

3. **Распределение по месяцам**:
   - Столбчатая диаграмма показывает активность по месяцам
   - Выявляет периоды наибольшей активности

4. **Распределение по дням недели**:
   - Показывает, в какие дни чаще публикуются объявления
   - Обычно больше активности в выходные

#### Выводы:
- Временные признаки могут быть важными для понимания паттернов продаж
- Сезонные колебания отражают периоды повышенного спроса

### 1.13. Проверка гипотез

#### Гипотеза 1: Тип топлива влияет на тип автомобиля

#### Код:
```python
fuel_vehicle = pd.crosstab(df_class[target_col_class], df_class['fuelType'])
```

#### Объяснение:
- `pd.crosstab()` - создает таблицу сопряженности (перекрестную таблицу)
- Показывает распределение типов автомобилей по типам топлива

#### Визуализация:
1. **Grouped bar chart** - группированные столбцы для каждого типа топлива
2. **Stacked bar chart** - процентное распределение (столбцы сложены)

#### Результат:
- Большинство автомобилей используют бензин (`benzin`) или дизель (`diesel`)
- Некоторые типы (например, `bus`) чаще используют дизель
- **ГИПОТЕЗА ПОДТВЕРЖДЕНА**: тип топлива связан с типом автомобиля

#### Гипотеза 2: Бренд влияет на тип автомобиля

#### Код:
```python
brand_vehicle = pd.crosstab(df_class[target_col_class], df_class['brand'])
sns.heatmap(brand_vehicle.T, annot=True, fmt='d', cmap='YlOrRd')
```

#### Объяснение:
- Heatmap показывает количество автомобилей каждого типа по брендам
- Цветовая интенсивность отражает количество

#### Результат:
- Разные бренды специализируются на разных типах автомобилей
- Например, некоторые бренды больше производят `limousine`, другие - `kombi`
- **ГИПОТЕЗА ПОДТВЕРЖДЕНА**: бренд является важным фактором

#### Гипотеза 3: Год регистрации влияет на тип автомобиля

#### Код:
```python
df_class['year_period'] = pd.cut(df_class['yearOfRegistration'],
                                 bins=[0, 1990, 2000, 2010, 2020, 10000],
                                 labels=['<1990', '1990-2000', '2000-2010', '2010-2020', '>2020'])
```

#### Объяснение:
- `pd.cut()` - разбивает непрерывную переменную на интервалы (категории)
- Создает временные периоды для анализа

#### Результат:
- Более новые автомобили (2010-2020) чаще представлены в объявлениях
- Некоторые типы (например, `cabrio`) чаще встречаются в определенные периоды
- **ГИПОТЕЗА ПОДТВЕРЖДЕНА**: год регистрации связан с типом автомобиля

#### Гипотеза 4: Цена влияет на тип автомобиля

#### Код:
```python
sns.boxplot(data=df_class[df_class['price'] < 50000], 
            x=target_col_class, y='price')
```

#### Объяснение:
- `boxplot` (ящик с усами) показывает распределение цен:
  - **Ящик** - межквартильный размах (25%-75%)
  - **Линия в ящике** - медиана
  - **Усы** - диапазон данных (с учетом выбросов)
  - **Точки** - выбросы

#### Результат:
- Разные типы автомобилей имеют разные ценовые диапазоны
- Премиум типы (`cabrio`, `coupe`) имеют более высокие цены
- Экономичные типы (`kleinwagen`) имеют более низкие цены
- **ГИПОТЕЗА ПОДТВЕРЖДЕНА**: цена является важным признаком для классификации

### 1.14. Экспорт подготовленных данных

#### Код:
```python
df_export_class.to_csv('autos_prepared_classification.csv', index=False)
```

#### Объяснение:
- `to_csv()` - сохраняет DataFrame в CSV-файл
- `index=False` - не сохраняет индекс строк (чтобы избежать лишнего столбца)

#### Выполненные операции перед экспортом:
1. Удалены строки с пропусками в целевой переменной
2. Обработаны выбросы (ограничены разумными диапазонами)
3. Заполнены пропуски в остальных признаках
4. Данные готовы для обучения моделей

#### Результат:
- Файл: `autos_prepared_classification.csv`
- Размер: 301,783 строк × 26 столбцов
- Данные очищены и готовы к использованию

---

## 📈 ЧАСТЬ 2: ЗАДАЧА РЕГРЕССИИ

### 2.1. Описание датасета

**Датасет:** `youtube_dataset.csv`  
**Размер:** 19,300 строк × 7 столбцов  
**Целевая переменная:** `Likes` (количество лайков)

#### Типы признаков:
- **Категориальные**: название видео, название канала, имя пользователя, текст комментария
- **Непрерывные**: количество лайков (целевая переменная)
- **Дата/время**: дата комментария
- **Идентификаторы**: уникальный ID комментария

### 2.2. Анализ целевой переменной (Likes)

#### Статистики:
- **Среднее**: 2,142.41 лайков
- **Медиана**: 22.00 лайков
- **Стандартное отклонение**: 11,795.95
- **Минимум**: 0
- **Максимум**: 540,334

#### Ключевое наблюдение:
**Большая разница между средним (2,142) и медианой (22)** указывает на:
- Сильно правостороннее распределение
- Наличие множества выбросов (вирусные комментарии)
- Большинство комментариев получают мало лайков, но некоторые получают очень много

#### Визуализация:

1. **Гистограмма распределения**:
   - Показывает правостороннее распределение
   - Большинство значений близко к нулю
   - Длинный "хвост" справа (выбросы)

2. **Гистограмма в логарифмической шкале**:
   - `np.log1p()` - логарифм от (x + 1)
   - Показывает более нормальное распределение
   - **Важно**: логарифмическое преобразование может улучшить качество регрессионных моделей

3. **Box Plot**:
   - Ясно показывает множество выбросов
   - Медиана близка к нижней границе ящика
   - Много точек за пределами усов (выбросы)

4. **Квантили**:
   - 99% комментариев имеют менее 41,772 лайков
   - Только 1% комментариев получает очень много лайков

#### Выводы:
- Распределение лайков сильно правостороннее
- Логарифмическая шкала показывает более нормальное распределение
- **Рекомендация**: использовать логарифмическое преобразование или ограничение выбросов

### 2.3. Анализ пропусков и дубликатов

#### Пропуски:
- `User Name`: 1 пропуск (0.005%) - незначительно

#### Дубликаты:
- Найдено: 139 полных дубликатов
- Удалено, размер уменьшился до 19,161 строк

#### Выбросы:
- Лайков = 0: 1,531 (7.99%) - нормально для социальных сетей
- Лайков > 100,000: 34 (0.18%) - вирусные комментарии
- Лайков > 10,000: 953 (4.97%) - популярные комментарии

#### Обработка:
- Выбросы ограничены 99-м перцентилем (41,772 лайков)
- Это предотвращает влияние экстремальных значений на модель

### 2.4. Анализ категориальных признаков

#### Количество уникальных значений:
- `Video Name`: 191 уникальных видео
- `Channel Name`: 117 уникальных каналов
- `User Name`: 15,623 уникальных пользователей
- `Comment`: 17,573 уникальных комментариев

#### Временной анализ:
- Диапазон дат: 2013-11-15 до 2020-09-09
- Извлечены признаки: год, месяц, день недели

#### Визуализация:
- Распределение комментариев по месяцам (сезонность)
- Распределение по дням недели (пики активности)

### 2.5. Группировка данных

#### Сводные таблицы:

1. **Среднее количество лайков по каналам**:
   - Топ-каналы: gotyemusic (9,914), Marshmello (9,210), Wiz Khalifa (7,854)
   - Популярные каналы генерируют больше лайков

2. **Среднее количество лайков по видео**:
   - Топ-видео: "Despacito" (13,120), "Gangnam Style" (12,213)
   - Вирусные видео привлекают более активную аудиторию

#### Выводы:
- Разные каналы имеют разную среднюю популярность комментариев
- Некоторые видео привлекают более активную аудиторию
- Сезонные колебания могут влиять на активность

### 2.6. Feature Engineering

#### Код:
```python
df_export_reg['Comment_length'] = df_export_reg['Comment'].astype(str).str.len()
```

#### Объяснение:
- Создан новый признак - длина комментария
- Может быть важным: длинные комментарии могут получать больше лайков

#### Экспорт:
- Файл: `youtube_prepared_regression.csv`
- Размер: 19,161 строк × 12 столбцов
- Данные готовы для обучения моделей

---

## 🤖 ЧАСТЬ 3: МАШИННОЕ ОБУЧЕНИЕ

### 3.1. Установка библиотек

#### Установленные библиотеки:
- **CatBoost** - градиентный бустинг с нативной обработкой категориальных признаков
- **XGBoost** - оптимизированный градиентный бустинг
- **LightGBM** - быстрый градиентный бустинг
- **YDF** (Yggdrasil Decision Forests) - библиотека деревьев решений от Google
- **Scikit-learn** - метрики и утилиты

### 3.2. Подготовка данных для классификации

#### Код:
```python
X_train_class, X_test_class, y_train_class, y_test_class = train_test_split(
    X_class, y_class, test_size=0.2, random_state=42, stratify=y_class
)
```

#### Объяснение:
- `train_test_split()` - разделяет данные на обучающую и тестовую выборки
- `test_size=0.2` - 20% данных для теста, 80% для обучения
- `random_state=42` - фиксирует случайность для воспроизводимости
- `stratify=y_class` - сохраняет пропорции классов в обеих выборках

#### Результат:
- Обучающая выборка: 241,426 строк
- Тестовая выборка: 60,357 строк
- Количество признаков: 16
- Количество классов: 8

**Зачем разделение:** Тестовая выборка используется для оценки качества модели на данных, которые она не видела при обучении.

### 3.3. Обучение моделей классификации

#### 3.3.1. CatBoost

##### Код:
```python
catboost_class = CatBoostClassifier(
    iterations=100,
    learning_rate=0.1,
    depth=6,
    loss_function='MultiClass',
    random_seed=42,
    verbose=False
)
catboost_class.fit(
    X_train_class, y_train_class,
    cat_features=categorical_features_class,
    eval_set=(X_test_class, y_test_class),
    verbose=False
)
```

##### Объяснение параметров:
- `iterations=100` - количество деревьев (итераций бустинга)
- `learning_rate=0.1` - скорость обучения (шаг градиентного спуска)
- `depth=6` - максимальная глубина деревьев
- `loss_function='MultiClass'` - функция потерь для многоклассовой классификации
- `cat_features=categorical_features_class` - **ключевая особенность**: CatBoost автоматически обрабатывает категориальные признаки

##### Как работает CatBoost:
1. **Градиентный бустинг**: последовательно строит деревья, каждое следующее исправляет ошибки предыдущих
2. **Обработка категориальных признаков**: использует специальный алгоритм (ordered boosting), который не требует предварительной кодировки
3. **Обучение**: минимизирует функцию потерь на обучающей выборке
4. **Валидация**: отслеживает качество на тестовой выборке для предотвращения переобучения

##### Результаты:
- **Accuracy**: 0.7881 (78.81%)
- **F1-score**: 0.7833
- **Время обучения**: 367.68 секунд

##### Почему CatBoost показал лучшие результаты:
- Нативная обработка категориальных признаков более эффективна, чем ручная кодировка
- Ordered boosting уменьшает переобучение
- Хорошо работает с несбалансированными данными

#### 3.3.2. XGBoost

##### Код:
```python
# Кодировка категориальных признаков
for col in categorical_features_class:
    codes, uniques = pd.factorize(X_train_xgb[col].astype(str))
    X_train_xgb[col] = codes
    mapping = {val: idx for idx, val in enumerate(uniques)}
    X_test_xgb[col] = X_test_xgb[col].astype(str).map(mapping).fillna(-1).astype(int)

# Кодировка целевой переменной
le_y = LabelEncoder()
y_train_class_enc = le_y.fit_transform(y_train_class)
y_test_class_enc = le_y.transform(y_test_class)

xgb_class = XGBClassifier(
    n_estimators=100,
    learning_rate=0.1,
    max_depth=6,
    random_state=42,
    eval_metric='mlogloss'
)
```

##### Объяснение:
- `pd.factorize()` - преобразует категориальные значения в числовые коды
- `map(mapping).fillna(-1)` - обрабатывает неизвестные значения (заменяет на -1)
- `LabelEncoder()` - кодирует целевую переменную (классы → числа)

##### Как работает XGBoost:
1. **Градиентный бустинг** с оптимизациями:
   - Параллельное построение деревьев
   - Регуляризация для предотвращения переобучения
   - Обработка пропущенных значений
2. **Требует числовые признаки**: поэтому нужна кодировка категориальных
3. **Эффективность**: оптимизирован для скорости и памяти

##### Результаты:
- **Accuracy**: 0.7065 (70.65%)
- **F1-score**: 0.7010
- **Время обучения**: 25.82 секунд

##### Почему хуже CatBoost:
- Ручная кодировка категориальных признаков менее эффективна
- Потеря информации при кодировке
- Но быстрее обучается

#### 3.3.3. LightGBM

##### Код:
```python
lgb_class = LGBMClassifier(
    n_estimators=100,
    learning_rate=0.1,
    max_depth=6,
    random_state=42
)
```

##### Как работает LightGBM:
1. **Градиентный бустинг** с особыми оптимизациями:
   - **Leaf-wise** построение деревьев (вместо level-wise)
   - Обработка категориальных признаков (но требует кодировки)
   - Эффективное использование памяти
2. **Скорость**: быстрее XGBoost на больших данных

##### Результаты:
- **Accuracy**: 0.7143 (71.43%)
- **F1-score**: 0.7119
- **Время обучения**: 24.12 секунд

##### Особенности:
- Быстрее всех обучается
- Хорошо работает на больших данных
- Требует кодировки категориальных признаков

#### 3.3.4. YDF (Yggdrasil Decision Forests)

##### Код:
```python
ydf_class = ydf.RandomForestLearner(
    label='vehicleType',
    num_trees=100,
    max_depth=6,
    random_seed=42
).train(train_df_ydf)

# Получение предсказаний
pred_raw = ydf_class.predict(test_df_ydf)
class_labels = ydf_class.label_classes()
y_pred_ydf = [class_labels[np.argmax(row)] for row in pred_raw]
```

##### Объяснение:
- `RandomForestLearner` - ансамбль деревьев решений (Random Forest)
- Автоматически обрабатывает категориальные признаки
- `predict()` возвращает вероятности для каждого класса
- `np.argmax()` - выбирает класс с максимальной вероятностью

##### Как работает YDF:
1. **Random Forest**: строит множество независимых деревьев
2. **Голосование**: каждое дерево "голосует" за класс, выбирается большинство
3. **Автоматическая обработка**: не требует предобработки данных
4. **Простота**: минимальная настройка параметров

##### Результаты:
- **Accuracy**: 0.6462 (64.62%)
- **F1-score**: 0.6199
- **Время обучения**: 108.17 секунд

##### Особенности:
- Простота использования
- Автоматическая обработка признаков
- Хорошо для быстрого прототипирования
- Но хуже по качеству, чем градиентный бустинг

### 3.4. Сравнение моделей классификации

#### Результаты:

| Модель | Accuracy | F1-score | Время (сек) |
|--------|----------|----------|-------------|
| **CatBoost** | **0.7881** | **0.7833** | 367.68 |
| LightGBM | 0.7143 | 0.7119 | 24.12 |
| XGBoost | 0.7065 | 0.7010 | 25.82 |
| YDF | 0.6462 | 0.6199 | 108.17 |

#### Метрики качества:

1. **Accuracy (Точность)**:
   - Доля правильных предсказаний
   - Формула: (TP + TN) / (TP + TN + FP + FN)
   - Простая и понятная метрика

2. **F1-score (F-мера)**:
   - Гармоническое среднее точности и полноты
   - Формула: 2 × (Precision × Recall) / (Precision + Recall)
   - Учитывает несбалансированность классов
   - `weighted` - взвешенное среднее по классам

#### Визуализация:
- Столбчатые диаграммы показывают сравнение моделей
- Легко видеть, что CatBoost превосходит остальные

#### Выводы:
- **Лучшая модель**: CatBoost (78.81% точности)
- Все модели показали хорошие результаты (>64% точности)
- CatBoost превосходит благодаря нативной обработке категориальных признаков
- XGBoost и LightGBM требуют предобработки, но быстрее обучаются

### 3.5. Обучение моделей регрессии

#### 3.5.1. CatBoost для регрессии

##### Код:
```python
catboost_reg = CatBoostRegressor(
    iterations=100,
    learning_rate=0.1,
    depth=6,
    random_seed=42,
    verbose=False
)
```

##### Результаты:
- **R²**: 0.3303 (33.03% дисперсии объясняется моделью)
- **MAE**: 1,831.72 (средняя абсолютная ошибка)
- **MSE**: 26,050,291.20 (средняя квадратичная ошибка)
- **Время**: 0.76 секунд

##### Метрики регрессии:

1. **R² (коэффициент детерминации)**:
   - Показывает, какая доля дисперсии целевой переменной объясняется моделью
   - Диапазон: от -∞ до 1
   - **1.0** - идеальная модель
   - **0.0** - модель не лучше, чем простое среднее
   - **< 0** - модель хуже среднего

2. **MAE (Mean Absolute Error)**:
   - Средняя абсолютная ошибка
   - Формула: mean(|y_true - y_pred|)
   - Показывает среднее отклонение предсказаний от реальных значений
   - Менее чувствительна к выбросам, чем MSE

3. **MSE (Mean Squared Error)**:
   - Средняя квадратичная ошибка
   - Формула: mean((y_true - y_pred)²)
   - Штрафует большие ошибки сильнее
   - Более чувствительна к выбросам

#### 3.5.2. XGBoost для регрессии

##### Результаты:
- **R²**: 0.2589 (25.89%)
- **MAE**: 2,059.90
- **MSE**: 28,827,319.60
- **Время**: 0.33 секунд

#### 3.5.3. LightGBM для регрессии

##### Результаты:
- **R²**: 0.2864 (28.64%)
- **MAE**: 1,975.63
- **MSE**: 27,754,870.00
- **Время**: 0.22 секунд

#### 3.5.4. YDF для регрессии

##### Результаты:
- **R²**: 0.3444 (34.44%) - **лучший результат!**
- **MAE**: 1,760.40
- **MSE**: 25,498,436.33
- **Время**: 0.67 секунд

### 3.6. Сравнение моделей регрессии

#### Результаты:

| Модель | R² | MAE | MSE | Время (сек) |
|--------|----|----|----|-------------|
| **YDF** | **0.3444** | **1,760.40** | **25,498,436** | 0.67 |
| CatBoost | 0.3303 | 1,831.72 | 26,050,291 | 0.76 |
| LightGBM | 0.2864 | 1,975.63 | 27,754,870 | 0.22 |
| XGBoost | 0.2589 | 2,059.90 | 28,827,320 | 0.33 |

#### Выводы:
- **Лучшая модель**: YDF (34.44% объясненной дисперсии)
- Все модели показали приемлемые результаты для задачи регрессии
- Задача предсказания лайков сложная из-за высокой вариативности данных
- YDF показал лучшие результаты благодаря эффективной работе с категориальными признаками

#### Почему результаты регрессии ниже, чем классификации:
- Задача регрессии сложнее: нужно предсказать точное число, а не класс
- Высокая вариативность целевой переменной (от 0 до 540,334)
- Много выбросов (вирусные комментарии)
- R² = 0.34 означает, что модель объясняет 34% вариативности - это неплохо для такой сложной задачи

### 3.7. H2O Flow

#### Что такое H2O Flow:
- Веб-интерфейс для работы с платформой машинного обучения H2O
- Позволяет создавать модели через визуальный интерфейс
- Не требует написания кода

#### Инициализация:
```python
h2o.init(ip="localhost", port=54321, nthreads=2, max_mem_size="2G")
```

#### Обученные модели:

1. **H2O Random Forest (классификация)**:
   - Accuracy: 0.1002 (10.02%) - низкий результат
   - Возможные причины: неправильная настройка или проблемы с данными

2. **H2O Gradient Boosting (классификация)**:
   - Accuracy: 0.0548 (5.48%) - очень низкий результат

3. **H2O Random Forest (регрессия)**:
   - R²: 0.2998 (29.98%)
   - MAE: 1,943.04

4. **H2O Gradient Boosting (регрессия)**:
   - R²: 0.3155 (31.55%)
   - MAE: 1,789.07

#### Выводы по H2O:
- Для регрессии показал приемлемые результаты
- Для классификации результаты низкие (возможно, требуется дополнительная настройка)
- Удобный веб-интерфейс для визуализации и анализа
- Полезен для быстрого прототипирования

---

## 📊 ВИЗУАЛИЗАЦИИ И ИХ ИНТЕРПРЕТАЦИЯ

### Графики для классификации:

#### 1. Распределение целевой переменной (vehicleType)
- **Тип**: Countplot + Pie chart
- **Что показывает**: Распределение классов (типов автомобилей)
- **Вывод**: Классы несбалансированы, нужно использовать взвешивание

#### 2. Матрица корреляции
- **Тип**: Heatmap
- **Что показывает**: Корреляции между числовыми признаками
- **Вывод**: Нет сильных корреляций, все признаки полезны

#### 3. Распределение цен по брендам
- **Тип**: Barplot
- **Что показывает**: Средняя цена по брендам
- **Вывод**: Премиум бренды значительно дороже

#### 4. Boxplot цен по типам автомобилей
- **Тип**: Boxplot
- **Что показывает**: Распределение цен для каждого типа
- **Вывод**: Разные типы имеют разные ценовые диапазоны

#### 5. Сравнение моделей (Accuracy и F1-score)
- **Тип**: Barplot
- **Что показывает**: Качество моделей
- **Вывод**: CatBoost превосходит остальные модели

### Графики для регрессии:

#### 1. Распределение лайков
- **Тип**: Histogram (обычная и логарифмическая шкала)
- **Что показывает**: Распределение целевой переменной
- **Вывод**: Сильно правостороннее распределение, много выбросов

#### 2. Boxplot лайков
- **Тип**: Boxplot
- **Что показывает**: Распределение с выбросами
- **Вывод**: Множество выбросов (вирусные комментарии)

#### 3. Квантили распределения
- **Тип**: Barplot
- **Что показывает**: Значения на разных квантилях
- **Вывод**: 99% комментариев имеют < 41,772 лайков

#### 4. Сравнение моделей (R², MAE, MSE)
- **Тип**: Barplot
- **Что показывает**: Качество моделей регрессии
- **Вывод**: YDF показал лучшие результаты

---

## 🎓 ВЫВОДЫ И РЕКОМЕНДАЦИИ

### Общие выводы:

#### 1. Качество моделей:

**Классификация:**
- CatBoost показал наилучшие результаты (78.81% точности)
- Все модели показали приемлемые результаты (>64%)
- Нативная обработка категориальных признаков дает преимущество

**Регрессия:**
- YDF показал лучшие результаты (34.44% объясненной дисперсии)
- Задача сложнее из-за высокой вариативности данных
- Все модели показали приемлемые результаты

#### 2. Особенности алгоритмов:

**CatBoost:**
- ✅ Нативная обработка категориальных признаков
- ✅ Высокое качество предсказаний
- ✅ Устойчивость к переобучению
- ❌ Медленнее обучается

**XGBoost:**
- ✅ Хорошая производительность
- ✅ Эффективная оптимизация
- ❌ Требует предобработки данных
- ❌ Медленнее LightGBM

**LightGBM:**
- ✅ Быстрое обучение
- ✅ Эффективен на больших данных
- ❌ Требует предобработки данных

**YDF:**
- ✅ Простота использования
- ✅ Автоматическая обработка признаков
- ✅ Хорошо для прототипирования
- ❌ Хуже по качеству, чем градиентный бустинг

#### 3. Рекомендации по использованию:

**Для продакшена:**
- CatBoost или XGBoost (высокое качество, надежность)

**Для быстрого прототипирования:**
- YDF или LightGBM (простота, скорость)

**Для визуального анализа:**
- H2O Flow (удобный веб-интерфейс)

### Технические выводы:

#### 1. Обработка данных:
- Важность очистки данных (пропуски, выбросы, дубликаты)
- Правильная обработка категориальных признаков критична
- Feature engineering может улучшить качество моделей

#### 2. Выбор метрик:
- Для классификации: Accuracy и F1-score (учитывает несбалансированность)
- Для регрессии: R², MAE, MSE (разные аспекты качества)

#### 3. Разделение данных:
- Train/Test split необходим для объективной оценки
- Stratify для сохранения пропорций классов
- Random state для воспроизводимости

### Практические рекомендации:

1. **Для улучшения качества моделей:**
   - Увеличить количество деревьев (iterations)
   - Настроить гиперпараметры (learning_rate, depth)
   - Использовать кросс-валидацию
   - Применить feature engineering

2. **Для работы с несбалансированными данными:**
   - Использовать взвешивание классов
   - Применить oversampling/undersampling
   - Использовать метрики, учитывающие несбалансированность (F1-score)

3. **Для работы с выбросами:**
   - Ограничить выбросы (например, 99-м перцентилем)
   - Использовать логарифмическое преобразование
   - Применить робастные метрики (MAE вместо MSE)

---

## 📚 ГЛОССАРИЙ ТЕРМИНОВ

- **EDA (Exploratory Data Analysis)** - исследовательский анализ данных
- **DataFrame** - структура данных Pandas (таблица)
- **Корреляция** - мера линейной зависимости между признаками
- **Мультиколлинеарность** - сильная корреляция между признаками
- **Выбросы (outliers)** - значения, сильно отличающиеся от остальных
- **Градиентный бустинг** - ансамблевый метод, строящий деревья последовательно
- **Random Forest** - ансамблевый метод, строящий деревья параллельно
- **Overfitting (переобучение)** - модель запоминает данные вместо обучения паттернам
- **Train/Test split** - разделение данных на обучающую и тестовую выборки
- **Accuracy** - доля правильных предсказаний
- **F1-score** - гармоническое среднее точности и полноты
- **R²** - коэффициент детерминации (доля объясненной дисперсии)
- **MAE** - средняя абсолютная ошибка
- **MSE** - средняя квадратичная ошибка

---

## ✅ ЗАКЛЮЧЕНИЕ

Работа демонстрирует полный цикл машинного обучения:
1. Исследовательский анализ данных (EDA)
2. Подготовка и очистка данных
3. Обучение различных моделей
4. Сравнение и оценка качества

**Основные достижения:**
- Проведен комплексный анализ двух датасетов
- Обучены и сравнены 4 различных алгоритма машинного обучения
- Достигнуто качество 78.81% для классификации и R² = 0.34 для регрессии
- Выявлены ключевые факторы, влияющие на целевые переменные

**Практическая ценность:**
- Результаты могут быть использованы для автоматической классификации автомобилей
- Модели могут помочь предсказать популярность комментариев на YouTube
- Методология может быть применена к другим задачам машинного обучения

---

**Автор:** Студент МГПУ Войт Иван Иванович  
**Дата:** 2024

